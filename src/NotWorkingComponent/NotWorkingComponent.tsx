import React from 'react';
import Component from "/Component";

const NotWorkingComponent = () => {

    return <Component /> // but cmd + click here results in only highlighting the import above
}
export default NotWorkingComponent
